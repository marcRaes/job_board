{% extends 'base.html.twig' %}

{% block title %}Hello JobController!{% endblock %}

{% block body %}
    <div class="bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 p-4">
        <h1 class="text-4x1 text-center">Job Board</h1>
    </div>

    <div class="container mx-auto px-4">
        <p class="mt-4"><a href="{{ path('app_job_new') }}" class="bg-blue-500 rounded-lg p-2 shadow-lg">Ajouter une offre</a></p>
        {% for offre in offres %}
            {% include 'job/_offre.html.twig' %}
        {% endfor %}
    </div>
{% endblock %}
