{% extends 'base.html.twig' %}

{% block title %}Hello JobController!{% endblock %}

{% block body %}
    <div class="container mx-auto px-4">
        {% for offre in offres %}
            <div class="bg-white rounded-lg shadow-lg my-4 hover:bg-purple-100 transition-colors duration-300">
                <a href="{{ path('app_job_show', { 'id': offre.id }) }}" class="block">
                    <div class="flex justify-between items-center mb-4 p-4">
                        <div class="flex-1">
                            <h3 class="text-xl font-bold text-gray-900">{{ offre.nom }}</h3>
                            <p class="text-gray-600">{{ offre.description }}</p>
                        </div>
                        <p class="text-gray-700 font-bold">{{ offre.salaire }} €</p>
                    </div>
                </a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
