{% extends 'base.html.twig' %}

{% block title %}Hello JobController!{% endblock %}

{% block body %}
    <div class="container mx-auto px-4">
        <p class="mt-4">
            <a href="{{ path('app_job_new') }}" class="bg-blue-500 rounded-lg p-2 shadow-lg hover:bg-blue-700">Ajouter une offre</a>
        </p>
        {% for offre in offres %}
            {% include 'job/_offre.html.twig' %}
        {% endfor %}
    </div>
{% endblock %}
